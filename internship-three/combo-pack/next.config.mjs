/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ['www.shrm.org', 'studentprojectguide.com', 'imageio.forbes.com', 'encrypted-tbn0.gstatic.com']
  }
};

export default nextConfig;
