import React from "react";

// This is the grid background of the hero section 

export default function GridBackgroundDemo() {
  return (
    <div className="absolute md:h-full h-[160%] brightness-[.3] w-full dark:bg-black bg-white dark:bg-grid-white/[0.2] bg-grid-black/[0.2] relative flex items-center justify-center">
      {/* Radial gradient for the container to give a faded look */}
    </div>
  );
}
